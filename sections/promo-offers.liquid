{%- if section.settings.background != settings.background and section.settings.background != 'rgba(0,0,0,0)' -%}
  {%- assign blends_with_background = false -%}
{%- else -%}
  {%- assign blends_with_background = true -%}
{%- endif -%}

<style>
  #shopify-section-{{ section.id }} {
    {%- if section.settings.background == 'rgba(0,0,0,0)' -%}
      {%- assign section_background = settings.background -%}
    {%- else -%}
      {%- assign section_background = section.settings.background -%}
    {%- endif -%}

    {%- if section.settings.text_color == 'rgba(0,0,0,0)' -%}
      {%- assign heading_color = settings.heading_color -%}
      {%- assign text_color = settings.text_color -%}
    {%- else -%}
      {%- assign heading_color = section.settings.text_color -%}
      {%- assign text_color = section.settings.text_color -%}
    {%- endif -%}

    --heading-color: {{ heading_color.red }}, {{ heading_color.green }}, {{ heading_color.blue }};
    --text-color: {{ text_color.red }}, {{ text_color.green }}, {{ text_color.blue }};

    --section-background: {{ section_background.red }}, {{ section_background.green }}, {{ section_background.blue }};
  }
</style>

<section class="sec--promo-offers section {% unless blends_with_background %}section--flush{% endunless %} {{ section.settings.css_class }}">
  <div class="section__color-wrapper">
    <div class="container {% unless blends_with_background %}vertical-breather{% endunless %}">

      <div class="promo-offers">
        <div class="promo-offers--row">
          <div class="promo-offers--content">
            {%- if section.settings.logo != blank -%}
              <img class="promo-offers--logo" loading="lazy" src="{{ section.settings.logo | img_url: 'master' }}" alt="{{ section.settings.logo.alt }}">
            {%- endif -%}

            {%- if section.settings.title != blank -%}
              <div class="promo-offers--content-title heading h3">
                {{ section.settings.title }}
              </div>
            {%- endif -%}

            {%- if section.settings.content != blank -%}
              <div class="promo-offers--content-content">
                {{ section.settings.content }}
              </div>
            {%- endif -%}
          </div>

          <div class="promo-offers--blocks">
            {%- if section.blocks.size > 0 -%}
              <div class="scroller">
                <div class="scroller__inner">
                  <div class="promo-offers--columns">
                    {%- for block in section.blocks -%}
                      {%- assign button_background = block.settings.button_background -%}
                      {%- assign button_text_color = block.settings.button_text_color -%}
                      <div
                        class="promo-offers--item{% if block.settings.is_featured %} is-featured{% endif %}" id="block-{{ block.id }}"
                        style="--primary-button-background: {{ button_background.red }}, {{ button_background.green }}, {{ button_background.blue }};
                        --primary-button-text-color: {{ button_text_color.red }}, {{ button_text_color.green }}, {{ button_text_color.blue }};">
                        <div class="promo-offers--item-header">
                          {%- if block.settings.caption != blank -%}
                            <div class="promo-offers--item-caption">
                              {{- block.settings.caption -}}
                            </div>
                          {%- endif -%}
                        </div>
                        <div class="promo-offers--item-info">
                          {%- if block.settings.title != blank -%}
                            <div class="promo-offers--item-title">
                              {{- block.settings.title -}}
                            </div>
                          {%- endif -%}
                          {%- if block.settings.text != blank -%}
                            <div class="promo-offers--item-text">
                              {{- block.settings.text -}}
                            </div>
                          {%- endif -%}
                          {%- if block.settings.sub_text != blank -%}
                            <div class="promo-offers--item-subtext">
                              {{- block.settings.sub_text -}}
                            </div>
                          {%- endif -%}
                        </div>

                        <div class="promo-offers--item-media">
                          {%- if block.settings.image != blank -%}
                            <img
                              src="{{- block.settings.image | img_url: 'master' -}}"
                              alt="{{- block.settings.image.alt -}}"
                              loading="lazy"
                              class="promo-offers--item-image">
                          {%- endif -%}
                        </div>

                        <div class="promo-offers--cta">
                          {%- if block.settings.button_text != blank -%}
                            <a href="{{ block.settings.button_link }}"
                              class="button button--primary promo-offers--btn">
                              {{ block.settings.button_text }}
                            </a>
                          {%- endif -%}
                        </div>

                        <div class="promo-offers--footer">
                          {{ block.settings.bottom_text }}
                        </div>
                      </div>
                    {%- endfor -%}
                  </div>
                </div>
              </div>
            {%- endif -%}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Promo offers",
  "class": "shopify-section--promo-offers",
  "settings": [
    {
      "type": "text",
      "id": "css_class",
      "label": "CSS class"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Heading"
    },
    {
      "type": "richtext",
      "id": "content",
      "label": "Content"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background",
      "label": "Background",
      "default": "rgba(0,0,0,0)"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "rgba(0,0,0,0)"
    }
  ],
  "blocks": [
    {
      "type": "offer",
      "name": "Offer",
      "settings": [
        {
          "type": "checkbox",
          "id": "is_featured",
          "label": "Is featured?"
        },
        {
          "type": "text",
          "id": "caption",
          "label": "Caption"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title"
        },
        {
          "type": "liquid",
          "id": "text",
          "label": "Text"
        },
        {
          "type": "liquid",
          "id": "sub_text",
          "label": "Sub-text"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "header",
          "content": "Button"
        },
        {
          "type": "text",
          "id": "button_text",
          "label": "Button text"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button link"
        },
        {
          "type": "color",
          "id": "button_background",
          "label": "Button background",
          "default": "rgba(0,0,0,0)"
        },
        {
          "type": "color",
          "id": "button_text_color",
          "label": "Button text",
          "default": "rgba(0,0,0,0)"
        },
        {
          "type": "liquid",
          "id": "bottom_text",
          "label": "Bottom text"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Promo offers",
      "settings": {},
      "blocks": [
        {
          "type": "offer",
          "settings": {
            "title": "Offer 1"
          }
        },
        {
          "type": "offer",
          "settings": {
            "title": "Offer 2"
          }
        },
        {
          "type": "offer",
          "settings": {
            "title": "Offer 3"
          }
        }
      ]
    }
  ]
}
{% endschema %}